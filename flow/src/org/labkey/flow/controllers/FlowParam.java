package org.labkey.flow.controllers;

public enum FlowParam
{
    actionSequence,
    compId,
    scriptId,
    runId,
    wellId,
    statusFile,
    refresh,
    redirect,
    graph,
    logId,
    experimentId,
    queryName,
    columnList,
    sampleId,
    sampleSetId,
    objectId,
    protocolId,
}
