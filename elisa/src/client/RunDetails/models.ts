
export type SelectOptions = {
    value: any,
    label: string
}

export type PlotOptions = {
    showCurve: boolean,
    showLegend: boolean,
    plateName: string,
    spot: number,
    samples: string[],
    controls: string[],
    xAxisScale: string,
    yAxisScale: string
}